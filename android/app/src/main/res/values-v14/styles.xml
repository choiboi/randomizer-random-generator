<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Main app styling -->
    <style name="Theme.Main" parent="@android:style/Theme.Light.NoTitleBar">
    </style>
    <!--<style name="Theme.Main" parent="@android:style/Theme.Holo.Light">-->
        <!--<item name="android:logo">@android:color/transparent</item>-->
        <!--<item name="android:actionBarStyle">@style/Theme.Main.Actionbar</item>-->
    <!--</style>-->

    <!-- Styling for Actionbar -->
    <style name="Theme.Main.Actionbar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/base_blue</item>
        <item name="android:titleTextStyle">@style/Theme.Main.Actionbar.TitleTextStyle</item>
    </style>

    <style name="Theme.Main.Actionbar.TitleTextStyle" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Styling for the dialog box -->
    <style name="Theme.Light.Dialog" parent="@android:style/Theme.Dialog">
        <item name="@android:windowBackground">@android:color/white</item>
        <item name="@android:textColor">@android:color/black</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>